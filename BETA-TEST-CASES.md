# กินอะไรดี — Beta Test Cases

ชุดกรณีทดสอบสำหรับผู้ทดสอบจริง ใช้บันทึกผล Pass/Fail ตามสิ่งที่เกิดขึ้นจริงเท่านั้น

## TC-01 เปิดแอป
1. เปิด Beta URL บน browser จริง
2. ตรวจว่าหน้าหลักโหลดครบและอ่านข้อความได้
3. ผลที่คาดหวัง: ไม่มีหน้าขาว/404 และปุ่มหลักกดได้

## TC-02 ไม่รู้เลย
1. กด “ไม่รู้เลย”
2. ทำ flow ต่อจนได้เมนู
3. ผลที่คาดหวัง: ระบบแนะนำเมนูได้โดยไม่ค้าง

## TC-03 เลือกเงื่อนไข
1. เลือกมื้อ/จำนวนคน/งบ/ประเภทอาหาร
2. ขอคำแนะนำ
3. ผลที่คาดหวัง: ได้ recommendation ที่ flow ทำงานครบ

## TC-04 เลือกใหม่
1. เมื่อได้เมนูแล้วกดเลือกใหม่/reroll หลายครั้ง
2. ผลที่คาดหวัง: หน้าไม่พังและยังใช้งานต่อได้

## TC-05 กินอันนี้ / ประวัติ
1. เลือกเมนูและกด “กินอันนี้”
2. เปิดประวัติ
3. ผลที่คาดหวัง: รายการที่เลือกปรากฏตามพฤติกรรมที่ออกแบบไว้

## TC-06 เมนูโปรด
1. กดชอบ/บันทึกเมนู
2. เปิดส่วนเมนูโปรด
3. ผลที่คาดหวัง: เมนูที่บันทึกแสดงถูกต้อง

## TC-07 แชร์
1. กดแชร์เมนู
2. ผลที่คาดหวัง: share flow เปิดได้ หรือ fallback ที่ออกแบบไว้ทำงาน

## TC-08 ตำแหน่ง — อนุญาต
1. เริ่มจากสถานะที่ยืนยันได้ว่า site/app ยังไม่ได้รับ Location หรือ reset permission ให้เกิดการตัดสินใจใหม่ได้
2. เปิดหาร้านใกล้ตัว
3. เมื่อ browser/OS ขอสิทธิ์ ให้เลือกอนุญาต Location
4. ยืนยันว่าหลังอนุญาต flow เดินต่อโดยไม่ค้าง/เด้งกลับ และหน้า nearby/search ใช้เส้นทางที่ต้องอาศัยตำแหน่งได้จริง
5. บันทึกหลักฐานที่แยกได้ว่าเป็น **allow-location path** เช่น permission prompt/state + หน้าหลังอนุญาต หรือ video ต่อเนื่อง
6. ผลที่คาดหวัง: แอปไม่ค้างและดำเนิน flow ร้านใกล้ตัวต่อได้หลังได้รับสิทธิ์

> การที่ Maps เปิดได้หรือเคยเห็นพิกัดเพียงอย่างเดียวไม่เพียงพอสำหรับ TC-08 PASS หากไม่สามารถ trace ได้ว่ารอบนั้นผู้ใช้อนุญาต Location ให้ Kinaraidee จริง

## TC-09 ตำแหน่ง — ปฏิเสธ
1. ปฏิเสธ Location
2. ผลที่คาดหวัง: แอปยังใช้งานได้และมี fallback ที่เข้าใจได้

## TC-10 Google Maps fallback
1. เลือกเมนูที่ไม่มี partner ที่ตรงกัน (ถ้าเกิดขึ้นจริง)
2. เปิดหาร้าน
3. ผลที่คาดหวัง: ไปยัง Maps/search fallback ได้

## TC-11 Feedback
1. เปิดหน้า Feedback
2. กรอกและส่งข้อมูลทดสอบที่ระบุว่าเป็น Beta test
3. ผลที่คาดหวัง: มีสถานะสำเร็จ/ผิดพลาดที่ผู้ใช้เข้าใจได้

## TC-12 Partner application
1. เปิดหน้าสมัครร้านพาร์ตเนอร์
2. กรอกข้อมูลทดสอบที่ระบุว่าเป็น Beta test
3. ผลที่คาดหวัง: ส่งได้หรือแสดง validation/error อย่างถูกต้อง

## TC-13 PWA
1. บนอุปกรณ์/browser ที่รองรับ ติดตั้งแอป
2. เปิดจากไอคอน
3. ปิด/เปิดใหม่
4. ผลที่คาดหวัง: app shell เปิดได้และไม่เกิดหน้าเสีย

## TC-14 Offline shell
1. เปิดแอปออนไลน์อย่างน้อยหนึ่งครั้ง
2. ปิดเครือข่าย
3. เปิดแอปอีกครั้ง
4. ผลที่คาดหวัง: ส่วนที่ถูกออกแบบให้ cache เปิดได้ โดยไม่อ้างว่าข้อมูลออนไลน์ใหม่ยังใช้ได้

## TC-15 404 recovery
1. เปิด path ที่ไม่มีจริงภายใต้ GitHub Pages
2. ผลที่คาดหวัง: recovery route ทำงานตามที่ออกแบบ ไม่ทิ้งผู้ใช้ไว้ที่หน้าว่าง

## อุปกรณ์ขั้นต่ำของรอบแรก
- Android Chrome: อย่างน้อย 3 รุ่น
- iPhone Safari: อย่างน้อย 2 รุ่น

## วิธีบันทึก defect
ทุก Fail ให้บันทึก:
- Test case
- รุ่นอุปกรณ์
- OS version
- Browser/version
- ขั้นตอนที่ทำ
- ผลที่เกิดจริง
- ผลที่คาดหวัง
- Screenshot/video ถ้ามี
- Severity: Blocker / Critical / Major / Minor

หากข้อมูลรุ่น/เวอร์ชันไม่ได้ถูกเก็บจริง ให้เขียน `not captured` แทนการเดา และห้ามนำหลักฐานจากคนละอุปกรณ์/คนละ session มารวมเป็น PASS เดียวโดยไม่ระบุขอบเขต

ห้ามทำเครื่องหมาย Pass โดยไม่ได้ทดสอบบนอุปกรณ์จริง
